export type Scripts = {
  headerScripts: {
    page: string;
    elements: string;
  }[];
  footerScripts: {
    page: string;
    elements: string;
  }[];
};

export type ThirdPartyScript = {
  page: string;
  elements: string;
};
